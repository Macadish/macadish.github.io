---
import { site } from '../data/site';

const year = new Date().getFullYear();
---

<footer class="site">
  <div class="row">
    <span>Macad·ish — home kitchen, Singapore</span>
    <span>Not selling yet</span>
    <a href="/menu/">Menu</a>
    <a href="/reservations/">Reservations</a>
    <a href="/journal/">Journal</a>
    <a href="/about/">About</a>
    <a href="/contact/">Contact</a>
    <a href="/#subscribe">Subscribe</a>
    <a href="/rss.xml">RSS</a>
    <span class="contact">@{site.instagram}</span>
    <span class="contact">{site.email}</span>
    <span class="year">© {year}</span>
  </div>
</footer>

<style>
  footer.site {
    padding: 2.5rem var(--edge) 4rem;
    border-top: 1px solid var(--rule);
    font-family: var(--f-mono);
    font-size: 0.66rem;
    color: var(--muted);
    line-height: 1.9;
  }

  .row {
    display: flex;
    gap: 1.5rem;
    flex-wrap: wrap;
    align-items: baseline;
  }

  .row a { color: var(--muted); }
  .row a:hover { color: var(--ink); }
  .contact { color: var(--ink); user-select: all; }
  .year { margin-left: auto; }

  @media (max-width: 620px) {
    .year { margin-left: 0; }
  }
</style>
